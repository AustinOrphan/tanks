# PR Media

This orphan branch holds PR evidence media—screenshots and GIFs—referenced from PR bodies throughout the repository. It is never merged into main; files are organized by PR under `pr<N>/` directories. This approach keeps the main branch and working tree clean while preserving visual evidence of deterministic behavior rendered from each PR's final state.
