{
  "id": "mapgen-jam-corridor-counts-the-board-frame",
  "file": "tools/mapgen/measure.ts",
  "find": "    if (x < wideRadius || y < wideRadius || x > width - wideRadius || y > height - wideRadius) continue;\n    for (const [di, dj] of OPPOSED) {",
  "replace": "    if (false) continue;\n    for (const [di, dj] of OPPOSED) {",
  "expect": "killed",
  "why": "Lets the board's own frame act as a corridor wall. `tankLattice` excludes any point within `radius` of the frame, so the wide lattice insets further than the tank lattice and the difference is a band along every edge -- with this guard removed that band becomes part of the narrow set, and the first version of this measure reported a 19.00-unit corridor on a board 20 units wide, at every tunnel length it was given. The rim of a board is not a corridor, and an empty board with no walls at all is the control that says so.",
  "tests": [
    "tools/mapgen/measure.test.ts"
  ],
  "killedBy": [
    "mapgen quality measures: bot jam corridors (issue #822) finds nothing on an open board, where the frame is the only narrow thing"
  ]
}
