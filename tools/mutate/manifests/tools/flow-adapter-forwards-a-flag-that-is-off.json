{
  "id": "flow-adapter-forwards-a-flag-that-is-off",
  "file": "tools/capture/flow-adapter.mjs",
  "find": "    ...FLOW_FLAG_IDS.filter((id) => variant.flags[id] === true).flatMap((id) => ['--flag', id]),",
  "replace": "    ...FLOW_FLAG_IDS.filter((id) => variant.flags[id] !== undefined).flatMap((id) => ['--flag', id]),",
  "expect": "killed",
  "tests": [
    "tools/capture/flow-adapter.test.ts"
  ],
  "killedBy": [
    "buildFlowArguments (issue #815) builds the recorder argv from the recipe, flags only when on, in the allowlist order"
  ],
  "why": "The baseline half of the matched pair says `pp1Roles: false` explicitly. Forwarding a flag that is off would switch the experiment on in both halves."
}
