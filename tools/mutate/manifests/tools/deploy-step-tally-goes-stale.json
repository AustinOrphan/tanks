{
  "id": "deploy-step-tally-goes-stale",
  "file": "docs/agent/commands-and-operations.md",
  "find": "`ci.yml`'s 12 checking steps** (`verify`: 8, `visual`: 4)",
  "replace": "`ci.yml`'s 10 checking steps** (`verify`: 7, `visual`: 3)",
  "expect": "killed",
  "tests": [
    "tools/workflows.test.ts"
  ],
  "why": "Restores the tally issue #693 found stale: 10 checking steps with `verify` 7, after the affected-entries mutation step and `Session lifecycle round trip` had made it 12. The sentence says it went stale unnoticed before, which is what a recomputed count is for; the old figure must fail against the workflow files.",
  "killedBy": [
    "canonical verification commands in workflows keeps the deploy-path checking-step tally recomputed from the workflows (issue #693)"
  ]
}
