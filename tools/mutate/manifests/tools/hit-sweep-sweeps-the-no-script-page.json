{
  "id": "hit-sweep-sweeps-the-no-script-page",
  "file": "tools/visual/hit-sweep.mjs",
  "find": "  if (state.javascript === 'off') return 'the no-script page: the collector runs as page script';\n",
  "replace": "  // this exclusion removed\n",
  "expect": "killed",
  "tests": [
    "tools/visual/hit-sweep.test.ts"
  ],
  "why": "Issue #710. The sweep stops excluding the no-script state. The collector is page script, so with scripting off that reading cannot measure a control, and the verdict fails a reading that measured nothing. Killed by the exclusion-map case and the swept-list case (1 more id).",
  "killedBy": [
    "hit-sweep.mjs: which surfaces the visual gate sweeps excludes exactly the developer, startup-failure, no-script and match-failure states, each for its rule",
    "hit-sweep.mjs: which surfaces the visual gate sweeps sweeps every other catalogue state, the endings included, then the two extra surfaces"
  ]
}
