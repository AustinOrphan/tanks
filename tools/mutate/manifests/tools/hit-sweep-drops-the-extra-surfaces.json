{
  "id": "hit-sweep-drops-the-extra-surfaces",
  "file": "tools/visual/hit-sweep.mjs",
  "find": "  return [...states.filter((state) => hitSweepExclusion(state) === null), ...HIT_EXTRA_STATES];\n",
  "replace": "  return states.filter((state) => hitSweepExclusion(state) === null);\n",
  "expect": "killed",
  "tests": [
    "tools/visual/hit-sweep.test.ts"
  ],
  "why": "Issue #710. The sweep drops the surface no screen state reaches: Settings with Reset stats armed, whose relabelled button is the one a player presses a second time. #686 measured it; without it the gate stops covering it. Killed by the swept-list case (the last id goes missing). Until issue #766 this also dropped the Controllers pane, which the catalogue now reaches as `screen.controllers`.",
  "killedBy": [
    "hit-sweep.mjs: which surfaces the visual gate sweeps sweeps every other catalogue state, the endings included, then the extra surface"
  ]
}
