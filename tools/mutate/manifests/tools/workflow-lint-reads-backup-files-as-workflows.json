{
  "id": "workflow-lint-reads-backup-files-as-workflows",
  "file": "tools/workflow-lint/lint.mjs",
  "find": "  return names.filter((name) => /\\.ya?ml$/.test(name)).sort();",
  "replace": "  return names.filter((name) => /\\.ya?ml/.test(name)).sort();",
  "expect": "killed",
  "tests": [
    "tools/workflow-lint/lint.test.ts"
  ],
  "why": "Issue #761 lints the files GitHub reads, which are the ones ending in .yml or .yaml. An unanchored match also lints an editor's ci.yml.bak or ci.yml.orig, so a stale copy of a workflow can fail the gate for a file GitHub never parses."
}
