{
  "id": "persistence-store-count-goes-stale",
  "file": "docs/agent/architecture.md",
  "find": "All seven stores in `GameStores`",
  "replace": "All six stores in `GameStores`",
  "expect": "killed",
  "tests": [
    "tools/instructions.test.ts"
  ],
  "why": "Restores the store count issue #693 found stale: 'six' after `GameStores` had grown a seventh store. The persistence paragraph is the one an agent reads before touching storage, and a wrong inventory there is how a new store skips the developer namespace; the count is now read from `createStores`, so the old word must fail.",
  "killedBy": [
    "the instruction files keeps the persistence counts in the agent docs recomputed from the stores (issue #693)"
  ]
}
