{
  "id": "capture-adapter-trusts-the-reported-arms",
  "file": "tools/capture/gallery-adapter.mjs",
  "find": "    if (report.arms === null || typeof report.arms !== 'object' || report.arms[key] !== requested[key]) {",
  "replace": "    if (false) {",
  "expect": "killed",
  "tests": [
    "tools/capture/gallery-adapter.test.ts"
  ],
  "killedBy": [
    "moment recipes naming developer arms (issue #775) records the arms the gallery applied, and refuses a report that applied something else"
  ],
  "why": "Provenance records what the gallery APPLIED, and a capture whose gallery applied something other than the recipe asked must fail rather than be filed under the recipe. Without the comparison the manifest would carry the requested arms in `requestedInputs` beside an applied set that disagrees, and nothing would say so."
}
