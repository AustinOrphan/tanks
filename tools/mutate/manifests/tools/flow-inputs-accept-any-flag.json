{
  "id": "flow-inputs-accept-any-flag",
  "file": "tools/screens/flow.mjs",
  "find": "    if (allowed === undefined) throw new Error(`flags.${id} is not a flow flag (${FLOW_FLAG_IDS.join(', ')})`);",
  "replace": "    if (false) throw new Error(`flags.${id} is not a flow flag`);",
  "expect": "killed",
  "tests": [
    "tools/screens/flow.test.ts",
    "tools/capture/schema.test.ts"
  ],
  "killedBy": [
    "flow inputs and the URL they build (issue #815) refuses a driver it has no policy for and a flag off the allowlist, and never a raw string",
    "capture recipe schema rejects unknown producer kinds and clearly refuses recognized unimplemented kinds"
  ],
  "why": "The flags a flow recipe may switch on are an allowlist checked against the dev-flag registry. Accepting any key would let a recipe put arbitrary parameters on the page's URL, which issue #815 forbids."
}
