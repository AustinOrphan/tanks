{
  "id": "entities-outside-the-motion-fanout",
  "file": "src/render/renderer.ts",
  "find": "      entities.setReducedMotion(on);",
  "replace": "      void entities;",
  "expect": "killed",
  "tests": [
    "src/render/motion-policy.test.ts"
  ],
  "killedBy": [
    "the reduced-motion fan-out (issue #651) routes the policy to EVERY render system that declares an answer to it"
  ],
  "why": "The entity layer owns three of this issue's five treatments -- the spawn frames, the mine fuse and the animated skin's drift -- so dropping it from the router silently un-does most of the change while every other reduced treatment still works. This is the same hole the barrel-recoil entry names, on the member this issue adds."
}
