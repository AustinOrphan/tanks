{
  "id": "shell-trail-folds-at-a-wall-beyond-its-reach",
  "file": "src/render/shell-trail.ts",
  "find": "    let tmax = best;",
  "replace": "    let tmax = Infinity;",
  "expect": "killed",
  "tests": [
    "src/render/shell-trail.test.ts"
  ],
  "killedBy": [
    "shell bounce-trail after a bounce (issue #774) leaves a bounced shell with no wall within its reach laid straight"
  ],
  "why": "Only a wall closer than the row can bend it. Without the reach bound, a bounced shell long past its corner folds its row at a wall many units behind it, leaping the dash across the board."
}
