{
  "id": "retarget-reason-never-goes-stale",
  "file": "src/render/ai-contact.ts",
  "find": "  if ((tank.aiRetargetAgeTicks ?? 0) > REASON_FRESH_TICKS) return '';",
  "replace": "  if (false) return '';",
  "expect": "killed",
  "tests": [
    "src/render/ai-contact.test.ts"
  ],
  "killedBy": [
    "contactLabel drops the reason once it is stale, so the label does not claim a switch that was seconds ago"
  ],
  "why": "Carries the retarget reason forever, so a label read at any moment says the tank just switched -- a diagnostic that is always true is worse than none, because it is read as current. The scoped test also pins the boundary itself (age exactly 30 still shows), which is the half a `>` versus `>=` slip would get wrong without changing any other case."
}
