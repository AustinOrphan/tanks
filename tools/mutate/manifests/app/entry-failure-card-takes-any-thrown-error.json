{
  "id": "entry-failure-card-takes-any-thrown-error",
  "file": "index.html",
  "find": "            } else if (e.filename && e.filename === entry.src) {",
  "replace": "            } else if (e.filename !== undefined) {",
  "expect": "killed",
  "tests": [
    "src/game/index-html-entry-failure.test.ts"
  ],
  "why": "Any script error counts as the bundle failing to run, so an extension's content script throwing during startup replaces a page that is loading fine with 'could not start'.",
  "killedBy": [
    "index.html entry bundle failure card (issue #770) ignores a failed image and an error thrown from any other script"
  ]
}
