{
  "id": "layout-binding-clash-never-refused",
  "file": "src/input/gamepad-profile.ts",
  "find": "    if (clashing.length > 0) continue;",
  "replace": "    if (clashing.length < 0) continue;",
  "expect": "killed",
  "why": "A binding onto another action's button must be returned to the profile's own button; letting it stand puts two actions on one button, and Fire on Confirm's button is the #494 leak.",
  "tests": [
    "src/input/gamepad-profile.test.ts"
  ],
  "killedBy": [
    "resolveEffectiveProfile (issue #754) refuses a binding onto another action's button, so Confirm can never also fire (#494)",
    "resolveEffectiveProfile (issue #754) never leaves two of the nine actions on one button, over every single and paired binding"
  ]
}
