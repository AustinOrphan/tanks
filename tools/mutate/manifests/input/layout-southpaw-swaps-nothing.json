{
  "id": "layout-southpaw-swaps-nothing",
  "file": "src/input/gamepad-profile.ts",
  "find": "      preset === 'southpaw' ? Object.freeze({ moveX: aimX, moveY: aimY, aimX: moveX, aimY: moveY }) : profile.axes;",
  "replace": "      preset === 'southpaw' ? profile.axes : profile.axes;",
  "expect": "killed",
  "why": "Southpaw is the movement and aim stick pairs swapped; a preset that reports itself applied while reading the sticks as shipped is a setting that does nothing.",
  "tests": [
    "src/input/gamepad-profile.test.ts",
    "src/input/gamepad.test.ts",
    "src/input/gamepad-menu.test.ts"
  ],
  "killedBy": [
    "resolveEffectiveProfile (issue #754) swaps the movement and aim pairs for Southpaw, and moves no button",
    "createGamepadReader: the player's layout (issue #754) drives with the right stick and aims with the left under Southpaw",
    "resolveEffectiveProfile on a catalogue profile (issue #754) swaps its own stick pairs under Southpaw, and binds onto a button only it names"
  ]
}
