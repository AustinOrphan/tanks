{
  "id": "run-total-line-never-hidden",
  "file": "src/game/hud.ts",
  "find": "    const run = outcomeData?.run;\n    if (!run) {\n      runTallyEl.classList.add('hud-run-tally--hidden');\n      return;\n    }",
  "replace": "    const run = outcomeData?.run ?? outcomeData?.attempt;\n    if (!run) {\n      runTallyEl.classList.add('hud-run-tally--hidden');\n      return;\n    }",
  "expect": "killed",
  "tests": [
    "src/game/hud.surfaces.test.ts"
  ],
  "killedBy": [
    "hud: every ending gets its own screen (issue #323) reports the whole campaign run beside the level attempt, on the two endings that finish one"
  ],
  "why": "Falls back to the ATTEMPT tally when no run was resolved, so every ending carries a second line that duplicates the first under a label claiming it is the campaign. Worse than showing nothing, and the shape a well-meaning \"avoid the empty element\" edit takes: the line is present, populated and plausible on screens that have no run at all."
}
