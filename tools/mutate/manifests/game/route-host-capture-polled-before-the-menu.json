{
  "id": "route-host-capture-polled-before-the-menu",
  "file": "src/game/route-host.ts",
  "find": "    menuPoller.poll(now);\n    // AFTER the poll, on the same frame (issue #754): see `RouteUi.pollBindingCapture`.\n    routeUi.pollBindingCapture();\n",
  "replace": "    routeUi.pollBindingCapture();\n    menuPoller.poll(now);\n",
  "expect": "killed",
  "why": "The capture reads pads after the menu poller on the same page frame. Read first, the press that completes a capture ends it before the poller runs, so the poller no longer stands aside and dispatches the same press as a menu action: a B chosen for Fire also backs out of the pane.",
  "tests": [
    "src/game/route-host.test.ts"
  ],
  "killedBy": [
    "createRouteHost: the gamepad menu poller (issue #494) rebinds Fire onto B from a controller alone, and the B that completes it does not also back out (issue #754)"
  ]
}
