{
  "id": "hud-layout-missing-from-the-panel-list",
  "file": "src/game/hud.ts",
  "find": "      aboutView,\n      layoutView,\n",
  "replace": "      aboutView,\n",
  "expect": "killed",
  "why": "`act` finds the pane a controller is acting in from this list. Left out, the Controller Layout pane is open with no active container, so a controller's Confirm and directions do nothing there, and the pane cannot be operated from a controller at all.",
  "tests": [
    "src/game/hud.controller-layout.test.ts"
  ],
  "killedBy": [
    "the controller layout pane (issue #754) is a panel a controller can act in: Confirm with nothing focused lands inside the pane",
    "the controller layout pane (issue #754) a controller's D-pad reaches the same controls, Cancel included while a capture waits"
  ]
}
