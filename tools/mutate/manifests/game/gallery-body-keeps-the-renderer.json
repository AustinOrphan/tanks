{
  "id": "gallery-body-keeps-the-renderer",
  "file": "src/game/gallery-workbench.ts",
  "find": "      handle.dispose();",
  "replace": "      void handle;",
  "expect": "killed",
  "tests": [
    "src/game/gallery-workbench.test.ts",
    "src/game/route-ui.test.ts"
  ],
  "why": "Closing the workbench must dispose its scene handle. Otherwise the WebGL renderer and its last build stay alive over a pane that is gone, once per visit.",
  "killedBy": [
    "the gallery workbench releases everything it holds (issue #730) disposes the handle once, cancels a playing frame, and empties the pane",
    "the gallery workbench is mounted with its pane and released with it (issue #730) mounts from the page link on open, and disposes the handle and empties the body on close",
    "the gallery workbench is mounted with its pane and released with it (issue #730) teardown disposes a pane that is still open, once"
  ]
}
