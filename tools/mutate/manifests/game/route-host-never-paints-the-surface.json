{
  "id": "route-host-never-paints-the-surface",
  "file": "src/game/route-host.ts",
  "find": "  const stopPainting = sm.onChange((location) => {\n    hud.setState(locationToHudSurface(location));\n    // The bed follows the route, and it is the ONLY thing that moves it: a session no\n    // longer touches the music at all, and its release is empty (`loop.ts`), so leaving\n    // gameplay is a suite CHANGE rather than a stop followed by nothing. That is issue\n    // #485's silent Main Menu -- the outgoing session used to set the menu suite from its\n    // own subscriber and then silence it from its own teardown, one call later.\n    //\n    // Pinned in two places, because one covers the route and the other the disposal:\n    // `route-host.test.ts`'s \"FOLLOWS the game\" drives gameplay, pause and the menu back,\n    // and `loop.test.ts`'s reboot cases count `musicStops` at zero across five real\n    // sessions started and retired over the whole route.\n    followMusic(location);\n    // Both Main Menu affordances that are claims about a SAVE rather than about a match --\n    // whether a run is there to continue, and how far the Levels grid may reach -- re-read\n    // on every arrival. Arrival is the right moment because the stores change while the\n    // player is somewhere else: a run ends and a level is cleared during gameplay, and a\n    // session is disposed on the way back here (issue #429), so the page must read them\n    // again rather than trust the last value anything painted.\n    if (location.kind === 'route' && location.route.kind === 'main-menu') {\n      paintContinue();\n      paintLevelSelect();\n    }\n  });\n  hud.setState(locationToHudSurface(sm.location));\n  // ...and the bed, for the same reason the surface is painted here: the page opens on a\n  // location it was never notified of. `loop.ts` carried the identical eager call for the\n  // session it used to be (\"this path bypasses sm.onChange\"), and dropping it would leave\n  // the screen the player actually lands on -- Launch, or Main Menu once Launch is\n  // dismissed -- as the one screen with no music. Nothing is audible until the launch\n  // gesture unlocks the context; setting the suite now is what makes that gesture start\n  // the menu bed already in the right world rather than switching on arrival.\n  followMusic(sm.location);\n",
  "replace": "  const stopPainting = sm.onChange((location) => {\n    followMusic(location);\n    if (location.kind === 'route' && location.route.kind === 'main-menu') {\n      paintContinue();\n      paintLevelSelect();\n    }\n  });\n  followMusic(sm.location);\n",
  "tests": [
    "src/game/loop.test.ts",
    "src/game/route-host.test.ts"
  ],
  "expect": "killed",
  "expectFailures": 15,
  "why": "The page stops telling the HUD which screen is showing. Before issue #428 a session owned this subscription, which was survivable only because a session existed from the first frame; with the page booting empty, a HUD that only a session ever painted shows the player nothing -- no splash, no Main Menu, and therefore no button with which to start the session that would have painted them. 9 of 458 across both scoped files (route-host.test.ts 37 + loop.test.ts 421), re-measured after the page took over the Main Menu paints and the Continue refresh moved into the same subscription; the replacement keeps that refresh so this entry still isolates the surface paint. The route-host case is the direct one; the loop.test.ts eight are every case that reads `rec.hudStates`, which is what makes this hard to satisfy accidentally: the surface has to reach the HUD for the splash, the menu, gameplay, pause and the outcome screens alike. Re-measured at 10 of 462 (route-host.test.ts 38 + loop.test.ts 424) once the disposal case joined route-host.test.ts: its live-painter control expects the route change before disposal to paint. Re-measured at 11 of 465 (route-host.test.ts 39 + loop.test.ts 426) with #318's route-host case in the file: a real HUD that is never painted has no Controllers button to open at Pause. RE-ANCHORED for issue #324's step S5: the arrival branch beside the surface paint gained the Levels grid alongside Continue, so the find string had to be re-cut around it. The replacement still keeps that whole branch, which is what leaves this entry isolating the surface paint and nothing else. Count re-measured on the new text: 14 of 495 (route-host.test.ts 62 + loop.test.ts 433), which the re-cut left where it was. Re-measured at 15 of 511 (route-host.test.ts 75 + loop.test.ts 436) for issue #324's step S7: the added case is the attach-ordering pin, which cannot compare the relaunch push against the route reset's paint when the paint never happens. The previous 14 still fail, so the failing SET was compared, not the number renumbered. The find/replace pair grew in issue #485: the same block now also carries the page's music, and the mutation deliberately KEEPS both followMusic calls so that what is removed is still exactly the surface painting -- a replacement that dropped the music too would fail the audio cases and stop measuring the thing this entry is named for. Re-measured at 14 for issue #279, and it went DOWN by one, which is the interesting direction. The three loop.test.ts cases this entry used to fail through asserted `getState() === 'main-menu'` after a finished versus match -- a surface paint. Rematch does not land on the menu at all: it reboots through `requestVersusSession`, so their replacements assert on the start seam and no longer depend on the surface being painted. Fewer tests observe this mutation because the behaviour they observed is gone, not because coverage was lost. COUNT REMEASURED (issue #323): +1, from the chained Launch-to-next-mission scenario added to `loop.test.ts`. That test walks the whole run, so it is caught by anything that breaks a phase transition, a status push or the run's continuity -- which is why it lands in several of these populations at once. The number moved because a test was added to a scoped file, which is the case an exact `expectFailures` exists to force a re-measure for, not a sign the mutation's reach changed."
}
