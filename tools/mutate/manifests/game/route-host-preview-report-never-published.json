{
  "id": "route-host-preview-report-never-published",
  "file": "src/game/route-host.ts",
  "find": "  if (previewDevApi !== null) deps.devConsole[DEV_CONSOLE_KEY] = previewDevApi;",
  "replace": "  if (false) deps.devConsole[DEV_CONSOLE_KEY] = previewDevApi;",
  "expect": "killed",
  "tests": [
    "src/game/route-host.test.ts"
  ],
  "why": "Issue #736: the preview is timed but `__tanks.bench()` is never published, so a device run has nothing to read.",
  "killedBy": [
    "createRouteHost: the ?bench=preview workload is page-scoped (issue #736) hands the preview a timing scheduler, and publishes a report of the frames it ran with no session",
    "createRouteHost: the ?bench=preview workload is page-scoped (issue #736) removes its report on dispose, and leaves an entry that replaced it alone"
  ]
}
