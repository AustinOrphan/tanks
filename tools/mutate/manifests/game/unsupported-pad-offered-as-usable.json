{
  "id": "unsupported-pad-offered-as-usable",
  "file": "src/game/hud.ts",
  "find": "        if (candidate.kind === 'gamepad' && unsupportedPad(candidate.padIndex)) {\n",
  "replace": "        if (false) {\n",
  "expect": "killed",
  "tests": [
    "src/game/hud.controls.test.ts"
  ],
  "why": "Issue #597. The Controllers pane offers a pad Tanks cannot read as an ordinary, enabled candidate again, as it did before: no 'not supported' label, no pointer at the reason, and a reassign listener that assigns a slot the reader samples as neutral every tick. Killed by the disabled-candidate case and the cannot-be-chosen case.",
  "killedBy": [
    "hud: controller assignment panel (docs/superpowers/plans/2026-08-17-controller-assignment.md) a pad the browser reports but Tanks cannot read (issue #597) is a DISABLED candidate, labelled as unsupported, pointing at a visible reason",
    "hud: controller assignment panel (docs/superpowers/plans/2026-08-17-controller-assignment.md) a pad the browser reports but Tanks cannot read (issue #597) cannot be chosen: pressing its candidate reassigns nothing"
  ]
}
