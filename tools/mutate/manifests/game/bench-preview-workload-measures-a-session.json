{
  "id": "bench-preview-workload-measures-a-session",
  "file": "src/game/bench.ts",
  "find": "    subject: 'preview',\n",
  "replace": "    subject: 'session',\n",
  "expect": "killed",
  "tests": [
    "src/game/bench.test.ts",
    "src/game/route-host.test.ts"
  ],
  "why": "Issue #736: the preview workload is declared a session workload, so the page never instruments the Customize preview and ?bench=preview times a gameplay driver instead.",
  "killedBy": [
    "BENCH_WORKLOADS: which loop each workload measures (issue #736) measures a session for versus-bots and the Customize preview for preview",
    "createRouteHost: the ?bench=preview workload is page-scoped (issue #736) hands the preview a timing scheduler, and publishes a report of the frames it ran with no session",
    "createRouteHost: the ?bench=preview workload is page-scoped (issue #736) removes its report on dispose, and leaves an entry that replaced it alone"
  ]
}
