{
  "id": "devcfg-multiset-goes-negative",
  "file": "src/game/dev-config-menu.ts",
  "find": "  const next = (counts.get(value) ?? 0) + delta;\n  if (next < 0) return selection;",
  "replace": "  const next = (counts.get(value) ?? 0) + delta;",
  "expect": "killed",
  "tests": [
    "src/game/dev-config-menu.test.ts"
  ],
  "killedBy": [
    "stepMultiset never goes below zero for a kind"
  ],
  "why": "A count below zero is not a thing the list can express -- the rebuild loop simply emits nothing for that kind -- so the readout would drift negative while the URL stayed the same, and the player would have to press plus twice to see the first change."
}
