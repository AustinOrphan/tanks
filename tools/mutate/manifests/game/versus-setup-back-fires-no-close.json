{
  "id": "versus-setup-back-fires-no-close",
  "file": "src/game/hud.ts",
  "find": "    closeSurface(VERSUS_SETUP_SURFACE);\n    closeVersusSetupSubscribers();",
  "replace": "    closeSurface(VERSUS_SETUP_SURFACE);",
  "expect": "killed",
  "tests": [
    "src/game/hud.surfaces.test.ts",
    "src/game/route-host.test.ts"
  ],
  "killedBy": [
    "createHud application transition contract reports the versus setup pane opening and closing exactly once per transition (issue #785)",
    "createRouteHost: Versus Setup reads the controllers already connected (issue #785) holds its hotplug listeners for exactly as long as the pane is up"
  ],
  "why": "Back is the pane's ordinary exit. Without the close, route-ui never removes the hotplug listeners it added on open."
}
