{
  "id": "practice-failure-reads-as-a-campaign-game-over",
  "file": "src/game/hud.ts",
  "find": "  'practice-failed': {\n    title: () => 'Level Failed',\n    subtitle: '',",
  "replace": "  'practice-failed': {\n    title: () => 'Game Over',\n    subtitle: 'Out of lives.',",
  "expect": "killed",
  "tests": [
    "src/game/hud.surfaces.test.ts"
  ],
  "why": "Collapses two of the five endings onto one screen -- the specific collapse issue #323 names, since a practice level and a campaign run are the two positions a player can hold and this makes the loss of one read as the loss of the other. A table keyed over the outcome kinds cannot prevent two entries agreeing, so the population sweep is what has to catch it; the per-ending case catches it too and names the strings, and both are listed because they fail for different reasons. RE-ANCHORED after the owner ruling renamed this ending to `Level Failed` and dropped its subtitle. The collapse being mutated is the same one: a failed practice level reading as a lost campaign.",
  "killedBy": [
    "hud: every ending gets its own screen (issue #323) gives each campaign and practice ending its own copy and its own action",
    "hud: every ending gets its own screen (issue #323) THE GAP (issue #323): no two endings render as the same screen any more"
  ]
}
