{
  "id": "route-host-start-cannot-pause",
  "file": "src/game/route-host.ts",
  "find": "    if (sm.isSimulating && action !== 'pause') return;\n    if (hud.act(action)) return;",
  "replace": "    if (sm.isSimulating) return;\n    if (hud.act(action)) return;",
  "tests": [
    "src/game/route-host.test.ts"
  ],
  "why": "Start is the one action dispatched while a session simulates; gating it with the rest means a pad can never pause a round.",
  "expect": "killed",
  "killedBy": [
    "createRouteHost: the gamepad menu poller (issue #494) Start pauses a simulating session and resumes a paused one; every other action is dropped mid-play"
  ]
}
