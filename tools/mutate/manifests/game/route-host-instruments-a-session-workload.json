{
  "id": "route-host-instruments-a-session-workload",
  "file": "src/game/route-host.ts",
  "find": "    benchFlag !== null && BENCH_WORKLOADS[benchFlag].subject === 'preview'",
  "replace": "    benchFlag !== null",
  "expect": "killed",
  "tests": [
    "src/game/route-host.test.ts"
  ],
  "why": "Issue #736: the page instruments the Customize preview for any workload, so a versus-bots run also times the preview and publishes a preview report that a session then overwrites.",
  "killedBy": [
    "createRouteHost: the ?bench=preview workload is page-scoped (issue #736) builds the preview with the window scheduler and publishes nothing for a session workload"
  ]
}
